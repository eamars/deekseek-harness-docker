# The current dsh CLI rejects --host 0.0.0.0 as a safety measure. This
# deployment patch binds the web server to the container interfaces while
# compose.yaml restricts the published host port to 127.0.0.1 by default.
- id: webserver
  config:
    host: 0.0.0.0
    port: 3080
