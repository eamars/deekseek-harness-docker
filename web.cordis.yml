# The current dsh CLI rejects --host 0.0.0.0. This deployment patch binds the
# web server to the container interfaces; compose.yaml publishes port 3080 on
# the Docker host interfaces for LAN access.
- id: webserver
  config:
    host: 0.0.0.0
    port: 3080
